# Tasks: full-sdk-migration

- [x] 1. Comments domain (facade + DTOs + consumers + test)
- [x] 2. Likes domain (facade + DTOs + consumers + test)
- [x] 3. Block domain (facade + DTOs + consumers + test)
- [x] 4. Notifications domain (facade + DTOs + consumers + test)
- [x] 5. User + auth domains (facades + DTOs + consumers + test)
- [x] 6. Final sweep: grep legacy refs, `npm test`, `typecheck`, `build`
