# Tasks: post-reporting

- [x] 1. Report facade + types in `app/shared/api/api.posts.ts` (reportPost, getMyReports, cancelReport; ReportType + reasons + payload/error helpers)
- [x] 2. Report dialog + hook UI (`useReportPost` hook, `ReportPostDialog` component, PostHeader menu wiring)
- [x] 3. Unit tests + verify (validation, classifier, facade mapping; `npm test`, typecheck, build green)
